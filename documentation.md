1. npm create vite@latest
2. how to approach this project from scrach
every project must have a main.jsx file under source
tailwindcss for styling the website
start the website: npm run dev

we need to make use of routing DOM from react.
whenever we cick something, it routes to a new page.
This will be the Navbar.jsx on the page

npm install @react-three/fiber for three.js fiber not plain three.js
Home.jsx has the 3D render
sketchfab for 3D models download as glb format

https://gltf.pmnd.rs/ from React Three Fiber team should upload it
make sure to include a line for assetsInclude for vite.config.js for glb files


npm install @react-spring/three to edit 3d model

feel free to update index.js in constant to fit your own needs
About.jsx takes mapping from index.s in constant folder which contain all images url and things

